# ============================================================
# Veritas Engine v0 — Zero-Hallucination Canadian Legal AI
# ============================================================

# Core ML
torch>=2.5.0
transformers>=4.48.0
datasets>=3.0.0
accelerate>=1.0.0
peft>=0.14.0

# Training
unsloth>=2025.1.0          # MoE LoRA training
trl>=0.12.0                # DPO/GRPO trainers

# Inference
outlines>=0.1.0            # Constrained decoding
lm-format-enforcer>=0.10.0  # Alternative grammar enforcement

# Data
pyarrow>=17.0.0
pandas>=2.2.0
numpy>=1.26.0

# Evaluation
scikit-learn>=1.5.0
evaluate>=0.4.0

# RAG (dual-index)
qdrant-client>=1.12.0

# Registry (in-memory Bloom filter)
pybloom-live>=4.0.0
mmh3>=5.0.0               # MurmurHash3

# Experiment tracking
wandb>=0.18.0

# Utilities
rich>=13.0.0              # Pretty logging
pydantic>=2.0.0           # Config validation
pyyaml>=6.0
python-dotenv>=1.0.0
tqdm>=4.66.0
